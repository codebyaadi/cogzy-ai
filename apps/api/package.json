{
  "name": "api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "format": "uvx ruff . --fix",
    "lint": "uvx ruff ."
  }
}
